# Git How To

# To add files to tree
git add -A

# To commit
git commit -m "Message"

# If origin not configured 
git remote add origin https://github.com/alertjean/stockscript.git

# To clone git repo
git clone https://github.com/alertjean/stockscript.git

# To push changes
git push -u origin master
